# Rewrite your code from the previous exercise to use a ternary expression.
import random
random_number = random.randint(0, 1)

print('Yes!' if random_number else 'No.')

"""
Ternary expressions are a concise way to choose between two values based on
some condition in Python. 

Its syntax is so:
expression1 if condition else expression2
"""
